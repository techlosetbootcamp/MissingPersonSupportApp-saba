
import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
    label: {
        color: '#000000',
        fontWeight: '500',
        fontSize: 14,
        marginBottom: 6,
      },
      input: {
        borderWidth: 1,
        borderColor: '#ccc',
        padding: 10,
        borderRadius: 5,
        marginBottom: 15,
        fontSize: 16,
      },
      buttonContainer: {
        flexDirection: 'row',
        justifyContent: 'center',
        marginTop: 10,
        gap: 10,
      },
});